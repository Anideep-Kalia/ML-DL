## Running AWS Bedrock on CLI
1. Download boto3 and awscli
2. Make user in IAM -> Create access key -> aws config -> copy-paste the keys
3. Now go to model access on bedrock page and request required models

Stable diffusion -> model for generating images

